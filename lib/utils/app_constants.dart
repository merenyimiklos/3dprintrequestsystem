import 'package:flutter/material.dart';

/// App-wide constants: colors, padding, labels.
class AppConstants {
  AppConstants._();

  static const String appName = '3D Print Queue';
  static const String appSubtitle = 'School 3D Printing Request System';

  /// Seed color for Material 3 color scheme.
  static const Color seedColor = Color(0xFF1565C0);

  static const double defaultPadding = 16.0;
  static const double smallPadding = 8.0;
  static const double largePadding = 24.0;
  static const double cardBorderRadius = 12.0;
}
